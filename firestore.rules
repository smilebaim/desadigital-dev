rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are completely open and are not secure.
    // They should only be used for initial development and testing.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
